import { config } from '@vue/test-utils'

// 全局配置
config.global.stubs = {}

// 如果需要，可以在这里添加全局组件、指令等
// 例如：config.global.components = { ... }
// 例如：config.global.directives = { ... }
// 例如：config.global.plugins = [ ... ]